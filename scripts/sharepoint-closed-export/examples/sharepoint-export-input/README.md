# Fake Multi-Site Export Input

This folder is a local example only. It contains fake TXT data that can be used to see the expected multi-site folder shape.

Do not copy these fake values into SharePoint or MongoDB. Replace the TXT files with real downloaded SharePoint files when preparing an actual export.

